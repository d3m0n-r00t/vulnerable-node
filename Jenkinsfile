pipeline {
    agent any
    stages {
        stage ("NPM Audit Analysis") {
            steps {
                sh 'npm install'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}